---
apiVersion: v1
kind: Service
metadata:
  name: python-app-service-prod
spec:
  type: NodePort
  selector:
    app: python-app-prod
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30009
